[build-system]
requires = ["setuptools>=61.2"]
build-backend = "setuptools.build_meta"

[project]
name = "hipace-tools"
version = "1.0"

[tool.setuptools]
py-modules = ["read_insitu_diagnostics"]
